# Quiz — Lesson 8.9 (SSR & Hydration)

1) What does `createSSRApp()` do compared to `createApp()`?
2) Which package provides `renderToString()` for Vue 3 SSR?
3) What is `onServerPrefetch()` used for?
4) Why must you avoid direct DOM access during SSR?
5) Which router history should be used on the server? On the client?
6) How do you transfer Pinia store state from server to client?
7) What is hydration in the context of SSR?
8) Name two common pitfalls when implementing SSR in Vue.
9) When would you choose Nuxt 3 over a custom Vite SSR setup?
10) What does `useSSRContext()` provide?
