# Quiz — Lesson 1.1 (Setup & Structure)

Answer these after completing the lesson and exercises.

1) What does `createApp(App).mount("#app")` do in a Vite/Vue project?
2) Which file contains the DOM element Vue mounts into by default?
3) Name two reasons Vite is faster than traditional bundlers in dev.
4) What is Hot Module Replacement (HMR), and how do you know it is working?
5) Where should you place shared assets like images or global styles in the scaffolded project?
6) How does `<script setup>` differ from the Options API in terms of boilerplate?
7) Why should you keep `strict` mode enabled in `tsconfig.json` for this course?
8) What is the role of Volar in the developer experience?
9) Describe a quick manual test to prove HMR is preserving component state.
10) What are two early code smells in a fresh project that you should avoid?
