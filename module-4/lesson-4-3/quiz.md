# Quiz: Route Parameters & Query Strings

**Module 4, Lesson 4.3** | 30 Questions

---

1. **b** - `/users/:id` (colon prefix)
2. **b** - `route.params.id`
3. **b** - `route.query.search`
4. **a** - Yes, use `props: true`
5. **b** - `:id?` (question mark)
6. **b** - `/:id(\\d+)` (regex in parens)
7. **b** - `route.hash`
8. **d** - All (string, array, undefined)
9. **b** - Number(route.query.page) || 1
10. **b** - Spread existing, add new
11. **a** - Yes
12. **b** - Params in path, query optional
13. **b** - Pagination state
14. **b** - { query: { ...route.query, page } }
15. **a** - Define scrollBehavior in router
16. **b** - Return savedPosition
17. **b** - No, need `props: true`
18. **c** - `/posts/:category/:id`
19. **b** - Use computed with conversion
20. **b** - With watcher on route.query
21. **b** - Params ignored with path
22. **a** - Type checking query params
23. **b** - Both work, props preferred
24. **b** - Regex pattern validation
25. **b** - { el: to.hash }
26. **a** - useDebounce
27. **b** - { query: {} }
28. **b** - As array of values
29. **b** - Preserve filter state
30. **b** - Delete undefined keys

**Passing**: 24/30 (80%)
